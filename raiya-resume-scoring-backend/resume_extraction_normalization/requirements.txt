# RAIYA Resume Extraction & Normalization
# Optimized for Python 3.12.0

# --- Core Dependencies ---

# Multi-format document extraction (PDF, DOCX, Images, URLs)
docstrange==1.1.8

# Data validation & Schema enforcement (Pydantic v2)
pydantic==2.12.5

# Environment variable management
python-dotenv==1.2.1

# User Interface (Premium Streamlit GUI)
streamlit==1.53.0

# --- Key Transitive Dependencies (pinned for 3.12 compatibility) ---

# Image processing (used by docstrange for OCR/rendering)
Pillow>=10.0.0

# Data manipulation (used by docstrange and streamlit)
pandas>=2.1.0
numpy>=1.26.0

# Networking & API calls
requests>=2.31.0
